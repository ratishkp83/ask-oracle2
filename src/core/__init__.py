"""Core safety, connectivity, and governance primitives for Ask Oracle Reports.

This package is the single source of truth for:
- SQL safety enforcement (`sql_safety`)
- Runtime safety limits (`config`)
- Connection profile storage with encrypted credentials (`profiles`)
- Secret-free audit logging (`audit`)
- Symmetric encryption helpers for secrets at rest (`crypto`)
"""
